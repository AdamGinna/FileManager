<Window x:Class="IntefaceFileManagerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:IntefaceFileManagerWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <local:FilePieChart Grid.Column="1" Grid.Row="0" />
        <local:BytesPieChart Grid.Column="2" Grid.Row="0" />
        <local:BytesColumnChart Grid.Column="2" Grid.Row="1" />
        <StackPanel Grid.Column="0">
            <Button Margin="6,6,6,6">Button 1</Button>
            <Button Margin="6,6,6,6">Button 2</Button>
            <Button Margin="6,6,6,6">Button 3</Button>
            <Button Margin="6,6,6,6">Button 4</Button>
            <Button Margin="6,6,6,6">Button 5</Button>
            <Button Margin="6,6,6,6">Button 6</Button>
        </StackPanel>
    </Grid>
</Window>
