<UserControl x:Class="IntefaceFileManagerWPF.ChooseDrive"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntefaceFileManagerWPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,5,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="4*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <ListBox x:Name="listBox" Grid.Row="0">
        </ListBox>
        <Button Grid.Row="1" Margin="0,5,0,5" Background="BlueViolet" Click="OnClick">
            <Button.Content>
                <Viewbox>
                    <TextBlock Foreground="White">Check</TextBlock>
                </Viewbox>
            </Button.Content>
        </Button>
        
    </Grid>
</UserControl>
